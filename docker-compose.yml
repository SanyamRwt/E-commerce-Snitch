services:
  backend:
    build: ./Backend
    env_file:
      - ./Backend/.env
  frontend:
    build:
      context: ./Frontend
      args:
        VITE_API_URL: ""
    ports:
      - "80:80"
    depends_on:
      - backend
